## Changes

- Fixes Codex live task clearing so completed plans do not reappear after pressing clear.
- Codex task sessions now use the latest `update_plan` timestamp for live activity instead of unrelated rollout log writes.
- Fully completed Codex plans now disappear from the Tasks pane instead of lingering as live sessions.
