name: Windows Release (Testing Edition)

on:
  push:
    tags:
      - "testing-*"
  workflow_dispatch:
    inputs:
      build_code:
        description: "Testing version (MAJOR.MINOR.PATCH, e.g. 8.2.69)"
        required: true

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-2022
            target: x86_64-pc-windows-msvc
            artifact_suffix: x64
          - runner: windows-11-arm
            target: aarch64-pc-windows-msvc
            artifact_suffix: arm64

    runs-on: ${{ matrix.runner }}
    defaults:
      run:
        working-directory: windows-tauri

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Derive build code
        id: build_code
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${{ inputs.build_code }}" ]]; then
            CODE="${{ inputs.build_code }}"
          else
            CODE="${GITHUB_REF_NAME#testing-}"
          fi
          if [[ ! "$CODE" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "build code must be MAJOR.MINOR.PATCH semver, got '$CODE'" >&2
            exit 1
          fi
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" && "${GITHUB_REF_NAME}" != "testing-${CODE}" ]]; then
            echo "tag '${GITHUB_REF_NAME}' does not match build code '${CODE}'" >&2
            exit 1
          fi
          echo "value=$CODE" >> "$GITHUB_OUTPUT"

      - uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa
        with:
          version: 11
          run_install: false

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: windows-tauri/pnpm-lock.yaml

      - name: Install Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8
        with:
          targets: ${{ matrix.target }}

      - name: Install WebView2 runtime check
        shell: pwsh
        run: |
          if (-not (Test-Path "HKLM:\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}")) {
            Write-Host "WebView2 not present, installing..."
            Invoke-WebRequest -Uri https://go.microsoft.com/fwlink/p/?LinkId=2124703 -OutFile MicrosoftEdgeWebview2Setup.exe
            Start-Process -FilePath .\MicrosoftEdgeWebview2Setup.exe -ArgumentList "/silent /install" -Wait
          }

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      - name: Build Tauri (${{ matrix.target }})
        env:
          # build.rs reads this into the binary as `LOOM_BUILD_CODE` so the
          # About panel and updater show the same semver as the tag.
          LOOM_BUILD_CODE: ${{ steps.build_code.outputs.value }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          TAURI_UPDATER_PUBLIC_KEY: ${{ secrets.TAURI_UPDATER_PUBLIC_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${TAURI_SIGNING_PRIVATE_KEY:-}" || -z "${TAURI_UPDATER_PUBLIC_KEY:-}" ]]; then
            echo "TAURI_SIGNING_PRIVATE_KEY and TAURI_UPDATER_PUBLIC_KEY are required" >&2
            exit 1
          fi
          node -e '
            const fs = require("fs");
            const path = "src-tauri/tauri.conf.json";
            const json = JSON.parse(fs.readFileSync(path, "utf8"));
            json.plugins.updater.pubkey = process.env.TAURI_UPDATER_PUBLIC_KEY;
            fs.writeFileSync(path, JSON.stringify(json, null, 2) + "\n");
          '
          pnpm tauri build --target ${{ matrix.target }}

      - name: Stage artifacts
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          out="release-artifacts"
          rm -rf "$out"
          mkdir -p "$out"

          bundle="src-tauri/target/${{ matrix.target }}/release/bundle/nsis"
          shopt -s nullglob
          installers=("$bundle"/*-setup.exe)
          if (( ${#installers[@]} != 1 )); then
            echo "expected exactly one NSIS setup installer in $bundle, found ${#installers[@]}" >&2
            printf 'found: %s\n' "${installers[@]}" >&2
            exit 1
          fi

          installer_name="$(basename "${installers[0]}")"
          artifact_name="${installer_name// /.}"
          cp "${installers[0]}" "$out/$artifact_name"
          pnpm tauri signer sign -p "${TAURI_SIGNING_PRIVATE_KEY_PASSWORD:-}" "$out/$artifact_name"
          test -s "$out/$artifact_name.sig"

          legacy_name="${artifact_name/_${{ steps.build_code.outputs.value }}_${{ matrix.artifact_suffix }}-setup.exe/_${{ matrix.artifact_suffix }}-setup.exe}"
          if [[ "$legacy_name" == "$artifact_name" ]]; then
            echo "could not derive legacy installer alias from $artifact_name" >&2
            exit 1
          fi
          cp "$out/$artifact_name" "$out/$legacy_name"
          cp "$out/$artifact_name.sig" "$out/$legacy_name.sig"
          find "$out" -maxdepth 1 -type f -print

      - name: Smoke test NSIS updater install
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $versionedInstaller = Get-ChildItem -Path "release-artifacts" -Filter "*_${{ steps.build_code.outputs.value }}_${{ matrix.artifact_suffix }}-setup.exe" | Select-Object -First 1
          $legacyInstaller = Get-ChildItem -Path "release-artifacts" -Filter "*_${{ matrix.artifact_suffix }}-setup.exe" |
            Where-Object { $_.Name -notmatch "_\d+\.\d+\.\d+_${{ matrix.artifact_suffix }}-setup\.exe$" } |
            Select-Object -First 1
          if (-not $versionedInstaller) {
            throw "No versioned ${{ matrix.artifact_suffix }} NSIS installer found in release-artifacts"
          }
          if (-not $legacyInstaller) {
            throw "No legacy ${{ matrix.artifact_suffix }} NSIS installer alias found in release-artifacts"
          }

          $programFilesX86 = [Environment]::GetEnvironmentVariable("ProgramFiles(x86)")
          $exeNames = @("loom.exe", "Loom Testing Edition.exe")
          $installDirs = @()
          if ($env:LOCALAPPDATA) {
            $installDirs += Join-Path $env:LOCALAPPDATA "Loom Testing Edition"
            $installDirs += Join-Path $env:LOCALAPPDATA "Programs\Loom Testing Edition"
          }
          if ($env:ProgramFiles) {
            $installDirs += Join-Path $env:ProgramFiles "Loom Testing Edition"
          }
          if ($programFilesX86) {
            $installDirs += Join-Path $programFilesX86 "Loom Testing Edition"
          }
          $pathsToCheck = foreach ($dir in $installDirs) {
            foreach ($name in $exeNames) {
              Join-Path $dir $name
            }
          }

          function Stop-LoomProcesses {
            Stop-Process -Name "loom" -Force -ErrorAction SilentlyContinue
            Stop-Process -Name "Loom Testing Edition" -Force -ErrorAction SilentlyContinue
            Start-Sleep -Seconds 2
          }

          function Invoke-InstallerSmoke {
            param(
              [Parameter(Mandatory = $true)] [System.IO.FileInfo] $Installer,
              [Parameter(Mandatory = $true)] [string[]] $Arguments,
              [Parameter(Mandatory = $true)] [string] $Label
            )

            Write-Host "Running $Label smoke test for $($Installer.FullName) $($Arguments -join ' ')"
            $process = Start-Process -FilePath $Installer.FullName -ArgumentList $Arguments -PassThru
            $deadline = (Get-Date).AddMinutes(4)
            $candidateRoots = @()
            while ((Get-Date) -lt $deadline) {
              $candidateRoots = @($pathsToCheck | Where-Object { $_ -and (Test-Path -LiteralPath $_) })
              if ($candidateRoots -and $process.HasExited) {
                if ($process.ExitCode -ne 0) {
                  throw "$Label install failed with exit code $($process.ExitCode)"
                }
                break
              }
              if ($process.HasExited -and $process.ExitCode -ne 0) {
                throw "$Label install failed with exit code $($process.ExitCode)"
              }
              Start-Sleep -Seconds 5
            }

            if (-not $candidateRoots) {
              if (-not $process.HasExited) {
                Stop-Process -Id $process.Id -Force -ErrorAction SilentlyContinue
              }
              Write-Host "Checked install paths:"
              $pathsToCheck | ForEach-Object { Write-Host "  $_" }
              if ($env:LOCALAPPDATA -and (Test-Path $env:LOCALAPPDATA)) {
                Get-ChildItem -Path $env:LOCALAPPDATA -Filter "*.exe" -Recurse -ErrorAction SilentlyContinue |
                  Where-Object { $_.FullName -match "Loom|loom" } |
                  Select-Object -First 20 -ExpandProperty FullName |
                  ForEach-Object { Write-Host "Found candidate: $_" }
              }
              throw "Installed Loom Testing Edition executable was not found after $Label install"
            }

            $exe = @($candidateRoots)[0]
            $productVersion = (Get-Item -LiteralPath $exe).VersionInfo.ProductVersion
            Write-Host "Installed executable: $exe"
            Write-Host "Installed product version: $productVersion"
            if ($productVersion -and -not $productVersion.StartsWith("${{ steps.build_code.outputs.value }}")) {
              throw "Installed product version '$productVersion' does not match ${{ steps.build_code.outputs.value }}"
            }

            Stop-LoomProcesses
            if (-not $process.HasExited) {
              Stop-Process -Id $process.Id -Force -ErrorAction SilentlyContinue
            }
          }

          Invoke-InstallerSmoke -Installer $legacyInstaller -Arguments @("/S") -Label "legacy silent"
          Invoke-InstallerSmoke -Installer $versionedInstaller -Arguments @("/S", "/R", "/UPDATE", "/ARGS") -Label "updater"

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: loom-testing-windows-${{ matrix.artifact_suffix }}
          path: windows-tauri/release-artifacts/*
          retention-days: 14

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/testing-')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          path: dist
      - name: Build latest-windows-testing.json
        shell: bash
        run: |
          set -euo pipefail
          BUILD_CODE="${GITHUB_REF_NAME#testing-}"
          if [[ ! "$BUILD_CODE" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "bad testing tag: ${GITHUB_REF_NAME}" >&2
            exit 1
          fi
          find dist -maxdepth 3 -type f -print
          NSIS_X64=$(find dist -name "*_${BUILD_CODE}_x64-setup.exe" | head -n1)
          NSIS_ARM=$(find dist -name "*_${BUILD_CODE}_arm64-setup.exe" | head -n1)
          SIG_X64="${NSIS_X64}.sig"
          SIG_ARM="${NSIS_ARM}.sig"
          for file in "$NSIS_X64" "$NSIS_ARM" "$SIG_X64" "$SIG_ARM"; do
            if [[ ! -s "$file" ]]; then
              echo "missing required Windows release artifact: ${file:-<empty match>}" >&2
              exit 1
            fi
          done
          SIG_X64_BODY=$(tr -d '\r\n' < "$SIG_X64")
          SIG_ARM_BODY=$(tr -d '\r\n' < "$SIG_ARM")
          NOTES="Loom Testing Edition ${BUILD_CODE}"
          cat > latest-windows-testing.json <<EOF
          {
            "version": "${BUILD_CODE}",
            "notes": "${NOTES}",
            "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "platforms": {
              "windows-x86_64": {
                "signature": "${SIG_X64_BODY}",
                "url": "https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}/$(basename "$NSIS_X64")"
              },
              "windows-aarch64": {
                "signature": "${SIG_ARM_BODY}",
                "url": "https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}/$(basename "$NSIS_ARM")"
              }
            }
          }
          EOF
          mv latest-windows-testing.json dist/
      - name: Publish pre-release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65
        with:
          files: dist/**/*
          prerelease: true
          generate_release_notes: true
          # release-testing.sh creates the pre-release with the DMG; this
          # step appends our NSIS assets to the same tag.
          fail_on_unmatched_files: false
